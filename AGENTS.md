# project-forge — 项目说明（会话自动注入）

本文件在每次会话开始时自动注入上下文。它是本 skill 与 AI 之间的接口契约，是项目的
唯一权威约定；任何修改必须遵守本文，并以「自检全绿 + 文档同步 + 提交存档」为完成标准。

<!-- project-forge:kernel:start -->
## 行事总纲（最高准则，优先于一切具体方法）

做任何事，先想清楚「根本」在哪里，再动手。追求一次解决一类问题，而不是反复处理
同一个问题的表象；追求整体清晰、可维护、可演进，而不是越堆越乱。这是所有行动的
最高准则，本文的一切方法都是它的落地方式。

### 四条准则

- **治本，不治标**：遇到问题先找根因，想清楚「怎样才算真正解决」再动手；修一次，
  就要让这一类问题不再复发——只把表面压下去，不算解决；
- **写原则，不写死**：能归为通则的，绝不写成个案特例；能用结构和机制表达的，
  绝不靠写死的细节——能做成配置、参数、规则表或自动探测的，就不把具体值、具体
  路径、具体名字硬编码进代码，环境变了、需求变了，代码不用重写；但动态化要真的
  服务于这个目的，不是为动态而动态。不依赖特定细节的做法，环境怎么变都不会失效
  ——通则即稳定；
- **融入结构，不打补丁**：任何新增（能力、规则、流程、工具用法）都要找到它在整体
  中的位置并融入进去，而不是在边上再挂一块；同一个问题第二次出现，说明上次治的
  是标，回头治根；
- **保持清朗，不堆屎山**：所有产物——方案、清单、目录、文件、流程、代码——都要
  结构清晰、职责分明、可维护可演进，改动时知道该动哪一处，而不是牵一发动全身；
  宁可多想一步，也不留下一堆难以理解、难以修改的堆积物。每次改动后自问：这次
  之后，还能不能在不弄坏别处的前提下接着往下写？答不上来，就是屎山又长高了
  一层，回退一步重做。

### 方案与决策

**先尽全力想出最优解**，这是默认动作：把请求做到能力范围内最好，直接说清最佳方案
是什么、为什么是它，不偷懒，不把决定权随手丢回去。

**给一个还是给几个，现场判断**，标准只有一条：这个选择是否取决于只有使用者自己
知道的实际情况。不是——只给最优解；是——给出几个方案让他挑。多方案必须是方向
不同、各自完整可用的平行路线（「都行得通，只是取向不一样」），不是从保守到激进
的梯子，也不拿明知不成立的选项凑数——一个稳妥但不成立的方案没有任何价值。不搞
「永远给一个」或「永远给三个」的套路。

**一次想到底**：设计必须一步到位、充分考虑清楚，不分期、不写「第一版先这样，然后
观察某某再调整」这类试探性措辞。（这条管**方案设计**；代码实现仍按「最小可用、
增量扩展」走，两者不矛盾。）

**取舍要说明影响**，且始终是 UX（使用者用起来顺不顺手）、DX（以后改、以后加功能
累不累）、AX（后续接手的人能不能看懂、能不能安全改动）三件事同时变好，并且不
破坏任何已有东西。需要取舍时说清楚对日常使用和后续维护的实际影响；普通取舍自己
决定，差别明显、或以后很难撤回的决定才交给使用者选。

### 动手前后四问（适用于任何任务）

前三条动手前问，第四条动手后问：

1. 我这是在治标，还是在治本？
2. 这个做法能覆盖一类问题，还是只覆盖眼前这一个？
3. 做完之后，整体是更清晰了，还是更臃肿了？
4. 我实际跑过了吗，凭什么说它真的成了？

### 工程落地纪律（改代码、写文档、新增能力时的具体法则）

- **简单成熟，不造轮子**：优先选简单、成熟、被验证过的方案；动手前先问
  「已有的能不能用」，避免过度设计、重复造轮子；
- **先看再动**：修改或新增前，先检查项目现有代码、依赖与文档，摸清现状
  与既有约定，再动手；
- **最小可用，增量扩展**：从最小可用版本起步，按真实需要逐步扩展，不为
  未来需求提前叠加复杂度；
- **模块清晰，职责单一，复用已有**：模块边界清晰、职责单一；优先复用
  已有能力，能并入现成的就不另起炉灶；
- **为长期维护决策**：架构决策考虑长期维护成本，避免只解决眼前问题的
  临时方案——今天的临时补丁，是明天要还的债；
- **小改动，保稳定**：修改前先理解现有实现，尽量小范围改动，确保已有
  功能稳定；能少动就不多动；
- **高效稳定**：既不慢也不脆——不在每次运行都重复做白工，也不让一次
  小异常把整个流程带崩；
- **修故障修根因，不糊一层**：先查清错误为什么发生再动手，修完这一类问题
  就不该再来；不许用「加个判断挡住它」「出错就重试一次」把症状往后推。
  被替换掉的旧逻辑一并清干净，不留两套做法并存；
- **不修好一个、弄坏一个**：每改一处，重新确认跟它相关的功能还正常；
  改完就宣布完成、不去看别处，等于把一个故障换成了一个更隐蔽的故障；
- **做完必须反复实测**：不能靠「看起来对了」——代码真跑一遍，改完确认真的
  好了、别处也没坏，脚本、配置、文档同样要过一遍；来回多验几遍，涉及好几处
  的地方每处都验。判断标准：拿得出实际跑过的结果才算完成，拿不出就是没做完；
- **不立碑，不写履历**：不为一次意外新增永久规则或纪念性注释——规则要来自
  真实的结构需要，而不是来自一次意外的惊吓；代码、脚本、配置、文档、文件名
  只写它现在做什么，不写改过什么、谁改的、旧版本什么样，也不写「以前是…现在
  改成…」这类履历——履历放提交记录里。判断标准：一个从没看过这个项目的人，
  只看这一份能不能正确理解？不能，就是里面混进了过时的东西。

任务编排、搜索调研、文件整理……一切具体方法，都是这条总纲的落地方式。

## 本文件的定位与编辑规则

**本文件的地位**：随仓库走，是**项目的契约**，不是某一个人的私有笔记。它描述
「这个项目是什么、要求什么」，而不是「谁在维护它」。这条区分决定了本文能写什么：

| 能写（对任何接手者都成立） | 不能写（随人而变） |
| --- | --- |
| 机制、架构、不变量、硬性规范 | 某个账号的用户名、邮箱、发布作用域 |
| 构建 / 测试 / 发布**该做什么、按什么顺序** | 推送目标、发布目标这些**取值** |
| 为什么这样要求（意图与边界） | 一次性的版本号、日期、某人当时的决定 |

**操作者取值一律现场取**：正文里凡是需要具体值的地方（仓库地址、项目标识、远端、
署名、发布目标），都从**项目自身的权威来源**读取，而不是写在本文里。可用的权威来源
随技术栈不同而不同，常见的几类：

- **清单与元数据文件**：项目标识、版本、依赖、发布范围、入口与产物声明；
- **版本控制配置**：远端地址、当前分支、提交署名（`git remote -v`、
  `git config user.name`、`git config user.email`）；
- **账号登录状态**：当前登录的账号与作用域（各生态的 `whoami` 一类命令）；
- **项目已有的约定文件**：既有文档、既有配置、既有目录结构所体现的事实。

上面只是常见类别，**不是固定清单**——项目里实际存在哪几个，就以哪几个为准；一个都
不存在时，说明该项目还没建立这些事实，那就先建立，再回填本文。**项目是这些事实的
唯一权威来源**：换人、换远端、换发布目标时，改这些真实配置即可，本文不需要跟着改，
也不会失准。

**载体名称不写死**：不同技术栈的清单文件、配置文件名各不相同。本文只写「从哪里取」，
不写「叫什么名字」；需要举例时举例，但举例不是规范——接手者用什么技术栈，就以那个
技术栈的权威来源为准。

**这条只管本文，不管给人看的文档**。README 与 CONTRIBUTING 面向使用者和贡献者，
要能直接复制粘贴，因此**必须写具体地址与命令**；易主时跟着改一次即可（改动集中在
安装命令与反馈渠道几处）。区分标准是**读者是谁**：读者是 AI（需要推理、会读项目）
就现场取值，读者是人（需要照做）就给现成命令。

**别人 fork 之后怎么办**：直接沿用即可。本文的机制与规范描述的是这个项目的做法，
对任何接手者同样成立；而所有「取值」都是现场读的，会自动指向他自己的仓库与账号。
他若想改工作方式（例如换分支模型、换 CI），按下面的编辑规则改本文即可——这正是本文
「是活的」的含义。**因此本文放在仓库里的意义不只是备份**：它是接手者（以及接手者的
AI）理解这个项目的最短路径；哪天仓库只剩本文和代码，也不会有「只有原作者才知道」的
隐性约定。

**本文件是活的**：它随项目演进被更新、增删、重构，不是一份永不变化的快照。会话中
若发现本文与代码现状不一致，以「本文意图 + 代码现状」为准，并按下面规则修正本文。

**AI 有权编辑本文件**。出现以下情况时，AI 应直接更新本文件（并提交到版本控制），
而不是绕过它、只做口头约定、或把特殊处理写死进代码：

- 规则过时或与代码行为不符；
- 用户要求调整项目约定；
- 发现本文无法覆盖的新场景，且该场景会反复出现；
- 本文表述不清、导致理解歧义。

**编辑本文件的小规矩**：

- 面向 AI 阅读：专业、准确、可执行；不写面向终端用户的科普内容（用户要求大白话时，
  在对话中解释，不写进本文件）。
- 与代码一致：修改项目行为后，若本文描述失准，须同步修正；发现过时描述应主动更新。
- 写意图不写快照：优先表达「为什么」与「边界」，让未来会话能推理到未覆盖的情形；
  避免堆砌一次性补丁式的例外条款，避免记录无意义的日期 / 版本号。
- 精炼：不重复、不冗余；结构清晰（标题层级 / 表格 / 代码块）。
- **引用他节的条目数不写数字**：写「G7 里那几件」，不写「G7 的三件事」——条目会随表格
  增减，而这类相对引用没有任何检查盯得住（数量检查只覆盖硬不变量、硬门控与脚本数）。
  确需指路时给节名，条目数以那一节为准。
- 遵守项目硬性规范（禁 emoji 等）；涉及用户可见能力或命令变化时，同步更新 README。

**文档同步**：README 面向人（能力 / 安装 / 使用 / 命令），本文件面向 AI（机制 / 规范 /
流程），两者描述同一项目。AI 修改项目或本文件后，应检查 README 是否仍准确，
不匹配时同步更新并提交。
<!-- project-forge:kernel:end -->

## 环境与上下文（以实际环境为准，不依赖固定值）

- **本 skill 不绑定宿主。** 它用的是通用的 `SKILL.md` 格式：入口文件加 `references/`、
  `templates/`、`scripts/` 三个同级目录；frontmatter 只用 `name` 与 `description` 这两个
  各家共通的核心字段；脚本是纯 Node，不调用宿主接口。因此凡支持该格式的宿主都能加载它。
  **不要把某个具体宿主的名字、环境变量或 skill 根路径写成前提**——写进本文件、README
  或任何产出，都会在换宿主那天变成假话。
- **位置由本文件所在目录直接推导**，不写死绝对路径；skill 整体迁移后相对关系不变。
- **资源引用一律用相对路径**（形如 `references/<文件名>.md`）。宿主会把它们解析到 skill
  自己的目录下，这是这套格式的一部分，也是它跨宿主可用的原因之一。写占位示例时用尖
  括号包住可变部分，这样自检不会把它当成一个真实存在的文件。
- 目录名必须与 frontmatter 的 `name` 逐字相同。
- 本项目零依赖：`scripts/` 只用运行环境内置模块，**只用 JavaScript**。**不引入第三方
  依赖，也不引入第二个运行时**——它是被 AI 在各种陌生项目里调用的工具，不能要求先装
  东西。README 对外承诺「Python 完全不需要」，那条承诺由 `preflight.mjs` 守住（它检查
  `scripts/` 下只有 `.mjs`）。
- **运行要求只有一项**：Node.js 16 或更高（下限来自 `String.prototype.replaceAll` 等
  语言特性）。`git` 可选——没有它的能力会如实报告并跳过，不会假装成功。README 的
  「环境要求」一节是这条要求的权威表述，改动运行要求时两处一起改。
- 生效规则：改 `SKILL.md`、`references/`、`templates/`、`scripts/` 后，下一次调用即可
  生效，无需重启任何服务。宿主对技能目录的变更会自动感知。

## 项目定位

一个通用的「项目锻造」skill：给任意项目目录，自己勘察现状、自己判断该做哪几件事，
把版本管理、远端仓库、文档套装、发布通道一次配到位。

它必须适用于各种技术栈（Node、Python、Rust、Go、Java、纯文档目录），以及插件类项目
（被宿主加载的扩展：已有专章见 `references/plugins/`，无专章按判定协议处理）
与 skill 项目本身。**判断依据永远是勘察出的事实，不是预设的清单。**

本项目自己就是它的第一个用户：本仓库的版本管理、文档与持续集成，都是用它自己的标准
配起来的。这不是宣传语，是**验收方式**——它教别人怎么做，自己就得那么做。

## 架构与关键不变量

### 四层结构（职责单一，互不复述）

| 层 | 目录 | 职责 | 读者 |
| --- | --- | --- | --- |
| 入口 | `SKILL.md` | 原则、能力矩阵、执行流程、硬门控 | AI，每次调用都读 |
| 决策 | `references/` | 按情境读的判据与方法 | AI，**按需读全文** |
| 落盘 | `templates/` | 可直接起步的文件骨架；`agents-kernel.md` 逐字注入 | 脚本与人 |
| 工具 | `scripts/` | 勘察、注入、自检 | 命令行 |

`references/` 与 `templates/` 必须分开：前者读来**决策**，后者拿来**落盘**。混在一起
最容易出的错是 AI 把本该逐字复制的内核「改写」一遍；分成两个目录从结构上压住了这个错。

### 关键不变量

1. **单一真相源**：通用内核只在 `templates/agents-kernel.md` 里存在一份，项目里的
   `AGENTS.md` 由 `scripts/compose-agents.mjs` 注入。**不得手抄**——手抄必然漂移，
   `preflight.mjs` 会报不一致。
2. **入口不复述细节**：`SKILL.md` 只写原则与指针，细节一律在 `references/`。两处都写
   机制，就一定会有两处不一致的那天。
3. **判定基于事实**：能力矩阵的输入是勘察脚本的产出。脚本读不到的字段一律留空，
   **不编造**——空的含义是「这项事实不存在」，此时的正确动作是去读项目或问用户。
4. **零依赖**：`scripts/` 只用内置模块。这条由 `preflight.mjs` 自动检查。
5. **不写死取值**：所有文档里凡是需要具体值的地方（账号、路径、版本号、仓库地址），
   一律写「从哪里取」。项目自身是这些事实的唯一权威来源。
6. **只读先行**：勘察未完成不写任何文件。这条是 skill 的第一硬不变量，它自己必须先做到。
7. **幂等**：所有脚本重复执行结果相同；`compose-agents.mjs` 只替换标记之间的内容，
   标记之外一个字节都不动。

## 构建与验证（改完必跑，全绿才算完成）

本项目没有构建步骤，验证就是自检加实跑：

```sh
node scripts/preflight.mjs                      # 结构自检：引用完整、内核一致、硬性规范
node scripts/selftest.mjs                       # 行为自检：造 fixture 实跑，断言判定结果
node scripts/compose-agents.mjs . --check       # 内核逐字一致、标记恰好一对、生成文件不缺节
node scripts/sync-toc.mjs README.md README.en.md --check     # README 目录与标题是否同步
```

`preflight.mjs` 检查的是「坏了就一定会出问题」的**结构**性质，不是风格偏好：

- `SKILL.md` 能否被宿主识别（frontmatter 合法、`name` 与目录名一致、名称形状正确）；
- `description` 是否会在会话技能目录里被截断（超出部分等于不存在）；
- 全文引用的每个文件是否真实存在（引用断链 = AI 按图索骥走到死路）；
- 每份 reference 是否有一级标题与「何时读本文件」节；
- `agents-kernel.md` 是否具备两个必需章节、许可证模板是否齐全；
- `AGENTS.md` 里的内核与模板是否逐字一致、总字节数是否在预算内；
- `README.md` 的目录与标题是否同步（见「派生内容」一节）；
- 全文是否含 emoji、BOM、构建机私有路径；
- `scripts/` 是否引入了非内置模块；
- 会实跑的脚本在只读或幂等模式下跑一遍（勘察、注入、行为自检、目录同步），
  语法错误、运行时崩溃都在这里暴露；`release-notes.mjs` 这类只在发版流程里执行的
  脚本不被自检覆盖，靠发版 CI 与 `--help` 自查兜底；
- 文档里的「数量自称」与实际是否相符（硬不变量、硬门控、脚本数），磁盘上的每个
  `references/`、`templates/`、`scripts/` 文件是否都登记在 `SKILL.md` 的文件地图里，
  `README` 与 `README.en.md` 是否把磁盘上的脚本与参考文档都列到了；
- `references/plugins/` 下每个专章是否都登记在 `plugin-project.md` 的索引里。

**单样本取值不回流进通用文档，这条没有自动检查，也不要试图加**：判断落在「这是通则
还是个案」上，不是字面特征。字面黑名单抓不住真正出问题的那次——往通用文档里注入
任何新的包名、存储键或账号它照样全绿，而它自身的名单又得逐条维护，还把个人身份
钉进了通用代码。加一个抓不住问题、又会叫喊的检查，比不加更坏（同「硬性规范」第 8 条）。

**`selftest.mjs` 是另一半，同样不可省。** 静态检查看不出「判定写错了」——survey 把
CMake 项目判成纯文档目录时，引用与格式全都正常。这类错误只能靠造 fixture 实跑发现，
而手工造一次就丢，下一个改动会把同样的错误再引入一遍。所以把它固化成常驻
检查：造 fixture、实跑、断言结果。

**本仓库自己也过一遍交付门禁**：`node scripts/review.mjs .`。它不是上面那四条之一——
它是给**目标项目**用的门禁，但本项目是它自己的第一个用户，所以这条自审必须诚实通过：

- 预期结论是「无缺失」。仓库里的夹具凡是「凭据形状」一律**运行时拼出**
  （见 `scripts/selftest.mjs` 顶部的 `FAKE_*` 常量），所以**不需要** `--secrets-reviewed`；
- 若它报了 `[缺]`，改真东西（源码、忽略规则、契约），**不要加 flag 把它压下去**；
- 只在本机存在的状态目录（被忽略、不进版本库）会落在 `[待问]`，那不是缺失。

**改动 `templates/agents-kernel.md` 后必须重新注入并提交**，否则自检会红。

### 派生内容用工具同步，不手工维护

`README.md` 里有两样东西**完全由别处决定**，因此不手写：

| 内容 | 由什么决定 | 怎么同步 |
| --- | --- | --- |
| **目录** | 各节标题 | `node scripts/sync-toc.mjs README.md README.en.md`（`--check` 只校验） |
| **徽章** | 外部服务返回的内容 | `node scripts/check-badges.mjs README.md README.en.md`（**写之前**先验一遍） |

目录由工具插在成对标记之间，**标记之外一个字节都不动**；加节、改名、删节之后重跑即
同步。`preflight.mjs` 与持续集成里都跑它的 `--check`——所以漂移会当场暴露，而不是等到
某个读者点了没反应。

**改 README 的结构后必须重跑 `sync-toc.mjs`**，不要手工编辑标记之间的内容（下次生成
会覆盖它）。

**双语说明文档成对维护**：`README.md`（权威）与 `README.en.md` 是同一份文档的两个版本，
改任何一份都算用户可见变化。改一份时必须打开另一份一起看，改完要能回答「另一份同步了吗」；
不一致时以中文权威版为准。不要机械对译。

**徽章不进 CI**：它依赖网络与外部服务，放进 CI 会因对方抖动而误报。验证走两条路：
写之前用 `node scripts/check-badges.mjs README.md README.en.md` 验一遍；
`review.mjs` 每次运行也会联网验（查不完时如实报「未核对」，不读成通过）。已知
**私有仓库上的 GitHub 系列徽章全部显示不出来**（星标、许可、发布），那种情况下
改用静态徽章。

这与「本文件的定位与编辑规则」里那条「写意图不写快照」是同一个道理：**快照会过期，
派生关系不会**——把派生关系交给工具，剩下的只有源头需要维护。

## 硬性规范

1. **禁 emoji**：代码、注释、文案、文档、提交信息一律不得出现 emoji 字符。由
   `preflight.mjs` 自动检查（它按 Unicode 的图形字符类别判定，箭头一类的符号不受影响）。
2. **零依赖**：`scripts/` 只允许 `node:` 前缀的内置模块。**只用 JavaScript**——不引入
   第二个运行时（这条同样重要：README 对外承诺「Python 完全不需要」，多一个 `.py` 脚本
   就让那句话变成假话，而使用者会照着自己的环境判断能不能用）。理由见「关键不变量」
   第 4 条。
3. **不写死取值**：见「关键不变量」第 5 条。
4. **入口不复述**：见「关键不变量」第 2 条。
5. **文档同步**：改动 `SKILL.md` 的结构、能力矩阵或流程后，检查 `README.md` 是否失准，
   失准即同步修正。两者描述同一件事，读者不同：README 面向人（这是什么、怎么用），
   `SKILL.md` 面向 AI（怎么判断、按什么顺序做）。
6. **改 skill 的行为 = 改它自己的验收标准**：`references/` 里写下任何新判据时，回头
   确认本项目自己是否满足它。不满足就先改自己——它是自己的第一个用户。
7. **不新增文件**：新增 reference 或 template 前先问「能不能并入现有的」。文件数量本身
   就是成本，读它的人（AI）要为此付出上下文预算。
8. **本 skill 不绑定任何宿主**。判断标准是**框定方式**：

   | 写法 | 判断 |
   | --- | --- |
   | 某类**被处理的项目**（本 skill 认识它、能正确处理它） | 允许——这是能力 |
   | **面向使用者的文档**（`README`）里出现任何具体宿主 | **禁止**——安装与使用说明不该让读者以为「这是给某个宿主用的」 |
   | 宿主布局当成**本 skill 的既定事实** | **禁止** |

   第一条是能力：换来的是「它认识这类项目」。后两条是束缚：换来的是「换个宿主就不成立」，
   而且会让读者把一个通用工具误认成某个生态的附属品。

   **`README` 是重灾区**，因为它面向的是「刚拿到这个 skill 的人」，他们最不需要知道
   某个具体宿主的目录布局——那是**宿主自己的文档**该回答的问题。README 只写
   「放进你所用宿主的用户级 skill 根」，并给出怎么找到那个位置（看已装的 skill、
   或查宿主文档）。**连「举例说明」都不要**：举一个具体宿主，整段就变成了那个宿主的
   说明，而读者可能是另一个宿主的用户。

   **写机制，不写取值**——与「不写死取值」是同一条准则的两种落法。宿主的**规则**
   是稳定的，值得写；宿主的**具体取值**（名字、环境变量、默认路径）会随配置与环境变，
   写进面向使用者的文档就是下一个坑。

   **这条没有自动检查，也不要试图加**：判断落在「框定方式」上，不是字面特征。
   字面启发式抓不住真正出问题的那次——出问题的原文里写着「以…为例」「默认为」
   照样被放行，同时还会对合法的命令误报。加一个抓不住问题、又会叫喊的检查，
   比不加更坏。

## 外部事实保鲜（防过期）

官方文档会变，本 skill 的文档不会自己变。**不要为此单开一个 update 流程**——多一套流程，
多一个要记得跑的东西，多一处会和主流程不一致的地方。保鲜长进现有结构里。

### 两类知识，两种写法

- **稳定的**（决策链、判据、安全红线）：照常写死，例如发布四问、密钥零入库、tag 与清单同源。
- **易变的**（字段名、数量上限、UI 路径、版本下限、附件名）：只写**查法 + 来源 +
  上次核对日期**——有权威链接就给链接，没有就写清「去哪个文档的哪一节查」，但**必须给出
  查法**：正文里的具体数字只是“上次看到的值”，不是真理，拿不准时按查法重核，不要背诵。

### 统一核对标记（实现是唯一真相源）

带外部易变事实的章节文件，必须在 `## 事实来源` 节末尾带恰好一个标记。**判据是「这个文件
承载了外部易变事实」，不是文件名**——目前包括 `references/publish-<生态>.md`、`references/plugins/`
下各专章，以及总纲 `references/publish.md` 与 `references/remote-github.md`（它们带着跨生态的
易变取值：制品库行为、令牌有效期这类），检查按文件清单要求，与文件名模式无关：

`<!-- <scope>-verified: date=YYYY-MM-DD [key=value ...] -->`

- `scope` 与文件名对应（`dsh.md`→`dsh`，`publish-npm.md`→`npm`）。`key=value` 可扩展，
  扩展键不拦，但每个键都要有**认领的含义**、值非空且单行：`host=` 的含义是「上次核对时
  **本机实际运行**的那套宿主版本」——不是兼容下界，也不是某个目标项目锁定的版本。含义写偏，
  比对就按错的语义吵；认领、归一化与比对**只实现一处**，`compose-agents.mjs` 与
  `preflight.mjs` 共用同一份——两处各判一次同类问题，迟早给出两种答案。
- **`host=` 的比对对象只有本机实际宿主**；跨机兼容性提示（把标记里那个版本与某个目标项目
  声明的版本对照，提示「本项目声明的范围可能早于核对过的宿主」）是**另一件事**：它可以是
  另一条消息、另一个调用点，但不得冒用 `host=` 的核对语义（那条消息里不许出现「核对结果」
  之类说法），也必须共用同一份归一化实现。
- **改 `host=` 必须与 `date=` 同批**：只改版本号不改日期，比对会喊「一致」而日期停在上一次
  核对日——那是最难发现的假绿。这条纪律由 `preflight.mjs` 查：它找出版本控制里最后一次改动
  `host=` 的提交，看那次提交是否同时改了 `date=`；取不到那次提交（值未提交、文件未跟踪、
  无可用历史）时报「未核对」，不静默也不失败。
- `scripts/preflight.mjs` 校验：节与标记双向缺一不可、scope 与文件名一致、日期真实且非未来、
  标记位于该节之后、每个 `key=value` 的形状合法。坏了即失败。
- **`host=` 与本机宿主版本的比对：能取到就比，取不到就报「未核对」，只警告不失败。**
  取值一律现场取——问宿主自己要，佐以它自己的安装元数据；**不写进文件**。写进文件的是快照，
  天然会过期，而「升级宿主后记得改」正是一条记性依赖的规则，机制要消灭的就是它。现场取的
  可靠是**有条件的**：只在这台机器装了该宿主时成立。所以取不到（未装、查询失败、同机存在
  多份该宿主而互相矛盾）一律报「未核对」：**「没比」与「比过且一致」必须可区分**，比不了就
  安静通过等于假装成功。取值途径按宿主各自登记，没登记就是「未核对」——**不为凑齐比对，
  把某个宿主的名字、命令或路径写进通用代码**。
- **不同不等于滞后，所以这条比对只警告不失败。** 换台机器校验、宿主回滚、同机存在多份该
  宿主，都能造出「不同」；而这些判断依赖本机装了什么、谁是当初核对的人——进了失败区就是
  「换台机器必红」，正是本项目要避免的那类检查。它只能喊出「你可能换了宿主，该重核了」；
  证明不了「外部事实仍然成立」（宿主没升级而官方文档改了，只能靠人查来源，或等失败驱动）。
- 超期只警告不失败：时间流逝不是破损，红了就是误报。阈值以 `scripts/preflight.mjs`
  中的保鲜常量为准（文档里不写死数字，写了就会漂移）；警告里会报出已过期天数与重核范围。

### 三个触发时机（事件驱动，不定时全量刷）

- **用时检查**：P5 落盘、P6 发布前，扫一眼相关标记超期没有，超了就现查官方文档再动手。
- **失败驱动**：发布报“无权 / 不存在 / 字段非法”时，第一动作是重核那一页，
  不是重试（四个专章的翻车表都按这个顺序；没有专章的生态按 `references/publish.md`
  的「发布失败与中断」表走）。
- **平时不管**：没超期、没失败，就别刷。刷了也是浪费，还制造提交噪音。

### 两条不做、以及为什么

- `compose-agents.mjs` 的滞后提醒只做 DSH：只有 DSH 的勘察产出了可比对的本地版本，
  其他生态没有这个数据。没有数据硬比对，就是误报源。不要为了“对称”把它推广出去。
- `review.mjs` 不查 skill 自身的标记年龄：它是目标项目的交付门禁，skill 的年龄与其无关；
  混在一起就是噪音。skill 的保鲜由 `preflight.mjs` 守。

### 方法自身的同步（后来加的东西也跑不掉）

- 新增 `references/publish-<生态>.md` 章节：`preflight.mjs` 按文件名模式要求它带节与标记，
  缺了即失败——不用记，检查会喊。
- 新增 `references/plugins/` 专章：模板骨架（`references/plugin-project.md` 第九节）
  要求写 `## 事实来源` 节并附统一标记；索引登记检查照旧。
- 改保鲜方法本身（阈值、格式、规则）：改 `scripts/preflight.mjs` 的实现，
  `scripts/selftest.mjs` 里有该判据的正反用例（好标记放行、坏日期/错 scope/超期各有断言），
  行为退化立刻可见。本节文字与实现不一致时，以实现为准并同步修正本节。
- **执行期的规则写在 `references/publish.md`**（『专章的「事实来源」标记』一节：动手前扫日期、
  失败先重核、核完改日期）。本节讲的是**维护期**的机制，两份不重复：执行者（在别人的项目里
  跑 P5/P6 的 AI）读的是 `references/`，读不到本文件——所以专章与专章模板一律指向那一节，
  而不再写「见 AGENTS.md」。

## 版本管理流程（必守）

本地仓库 + 远端。远端地址、当前分支、署名一律现场读（`git remote -v`、
`git branch --show-current`、`git config user.name`），不在本文件里写死。

**日常闭环**（用户描述需求即触发）：

1. 改文件；
2. 跑「构建与验证」里的四条命令，全绿；
3. `git status` 确认改动范围，`git add -A && git commit`；
4. `git push`。

**提交纪律**：一次实质变更一个提交；**提交信息要说清「更新了什么、修复了什么」**——读它的人
（包括发版时写发布说明的人）要能据此讲出使用者听得懂的一句话。中文或英文均可，**禁用 emoji**。
面向使用者的改动（命令变了、行为变了、README 变了）**不要用内部编号、任务代号或流程名充当
说明**（「18 条审计发现」「t3 分派三条」这类只对当事人有意义，出了这个上下文就没人读得懂）；
一次提交里有多件事时，主题行说最主要的那件，其余进正文。若改了
`templates/agents-kernel.md`，必须同一次提交里带上重新注入后的 `AGENTS.md`。

**取值现场取**：远端、分支、署名不写进本文件。改 remote、换账号时改真实配置即可。

**先确认角色**（涉及推送或发版前做一次）：远端指向正本仓库 = 维护者，走全部流程；
指向 fork 或没有远端 = 贡献者——改代码、跑自检照做，到此为止：推自己的分支并开改动请求，
不推主干、不打标签（没有权限，且请求事件天然拿不到 Secrets，需要钥匙的步骤不会跑也不会泄）。
判断不了时按贡献者走。贡献者流程见 `CONTRIBUTING.md`。

### 版本节点与发布说明

本项目**不使用版本号**，也**不出制品**：它是个 skill，靠克隆分发，没有包管理器的版本
概念。日常与发版是两条线：**日常只提交推送不打标签；只有明确说「发版」才打标签写说明**。
这样仓库里不会攒出一堆碎标签——标签是里程碑，不是提交的附庸。

**日常（默认，用户描述需求即触发）**：改完、验完、提交、推送，到此为止。**发版时那段「更新了
什么、修复了什么」是从提交信息攒出来的**，所以用户可见的改动（命令变了、行为变了、README 变了）
必须当场把提交信息写清楚——写「改了什么、影响谁」，不要写内部编号与流程名；发版那天再来
翻译一遍，等于把这件事重复做一次，而且那时已经想不起当时的语境。只改内部文档（本文件、
CONTRIBUTING）连攒都不用特意记。

**发版（只有明确说「发版 / 发出去 / 发一版」才触发）**：

1. 回顾上个标签之后攒了哪些用户可见变化（`git log <上个标签>..HEAD`，判据见 `references/publish.md`：
   **使用者会不会因此看到变化**）；
2. 定新号（`v0.x.y` 单调递增；不要与日期方案混用）；
3. 打标签并推送；
4. CI 自动写发布说明（见下），回读 Releases 页确认；
5. 汇报只说版号与去哪看。

**发布说明由 CI 自动写**：推 `v*` 标签 → `release.yml` 建 Release；**已存在则更新正文并回读比对**
（内容不一致就报错退出，job 直接红）。
**机制以 `.github/workflows/release.yml` 为准，这里不复述**（正文由
`scripts/draft-release-notes.mjs` 起草中文，再由 `scripts/release-notes.mjs` 按字节发送并回读比对，
不是平台按提交记录生成的模板）。凭证用仓库 Secrets 里的
`RELEASE_TOKEN`（细粒度 token，仅 Contents 读写、仅本仓库）；token 过期或换人时只换
Secrets 里那一个值，代码不动。标签推得比 job 早、
或 job 失败时，用 `workflow_dispatch` 手动触发一次并填标签名补漏——不要重打标签。
**重跑会用起草结果覆盖已有 Release 的正文**；发布页上有人工改写时不要重跑。

**标签命名**用 `v0.x.y`（序号语义，延续 `v0.1.0`）：本项目没有版本号可以对齐，所以命名规则自定，
只要**单调、不重复、可排序**即可。不要与日期方案混用——混用即不可排序。

**这一版更新了什么、修复了什么，必须写清楚**：发布说明就是给使用者看的更新说明。要写实际
改了什么、影响谁、他会不会因此需要动手，用使用者能看懂的话；**别复制提交主题当正文**，
也别堆内部编号与流程名（「18 条审计发现」「t3 分派三条」这类是给维护者看的，使用者读不懂）。
不影响使用者的内部重构不必写进正文。

**提交信息永远是发布说明的素材，所以它本身就要面向使用者**：一句话说清「改了什么、影响谁」，
不写内部编号、不写流程名。等发版时才想「这段该怎么讲给使用者听」，说明提交信息当时就没写清
——那时已经接不上了。同一批改动跨多次提交时，发布说明由**维护者补一段面向使用者的总述**
（照 `references/remote-github.md` 的规矩用脚本写进 UTF-8 文件），**不要让自动起草的提交
清单直接充当正文**。

**发布说明必须用脚本写，不手工拼命令行**（手动补写时的路径；通常走 CI 自动）：正文含中文，而 shell 的引用与编码规则因平台
而异，损坏的共性是**接口仍返回成功**、只有打开页面才看得出。做法与
`references/remote-github.md` 里给目标项目的那条一致——正文写进 UTF-8 文件，由脚本按
字节发送，发送后回读比对。

**教别人的规则自己先用**：`references/` 里对目标项目的发布说明要求（写清改了什么、
影响谁、不用内部编号），本文件对本项目同样生效，不设例外。

## 测试（约定）

本项目的测试是**自检加实跑**，没有测试框架：

- 结构性契约（引用完整、内核一致、规范合规）由 `scripts/preflight.mjs` 自动检查，
  它是常驻防线，改动后必跑；
- 行为性契约（勘察判定是否准确、注入是否幂等）靠**用 fixture 实跑**验证：造几种
  形态不同的临时目录，跑一遍勘察看分类是否正确、跑两遍注入看结果是否相同。
  fixture 用完即删，不留在仓库里——它是一次性的验证手段，性质上属于可再生成的产物。

新增判定能力时，**先造出能证伪它的 fixture**：一个该被识别成 X 的目录，和一个容易被
误判成 X 的目录。只验证正面案例，等于没验证。

## 本项目的报告位置

这是本文件面向 AI 的部分与面向人的部分的交界，特此说明：`README.md` 讲这个 skill
是什么、怎么用、有哪些 reference；本文件讲它内部的约定与纪律。**两者不得互相复述。**

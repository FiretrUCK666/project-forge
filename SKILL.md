---
name: project-forge
description: |
  把一个项目目录锻造为规范项目：版本管理、远端仓库、文档套装、发布通道一次配齐。
  触发词：弄版本管理、配 git、上仓库、建仓库、推到 GitHub、初始化项目、项目起步、
  新项目配置、标准化项目、规范项目、整理项目、给项目弄好、写 AGENTS.md、写 README、
  写贡献指南、配 LICENSE、配 CI、发到 npm、发布。
  覆盖各种技术栈：Node、Python、Rust、Go、Java、纯文档目录，以及插件类项目与 skill 项目。
  只读勘察先行，按事实判定该做与不该做；幂等、可重复执行；默认私有仓库。
---

# project-forge

把一个项目目录锻造成规范项目。给一个目录，自己勘察现状，自己判断该做哪几件事，然后
一次做到位。

## 何时使用

用户提到下面任何一类需求时使用。触发不要求用户说出本 skill 的名字——按意图判断。

- **版本管理**：弄版本管理、配 git、上仓库、建仓库、推到 GitHub、初始化项目、项目起步；
- **文档套装**：写 AGENTS.md、写 README、写贡献指南、配 LICENSE；
- **发布通道**：发到 npm、发布、配 CI；
- **笼统说法**：标准化项目、规范项目、整理项目、把项目配置好。

不必使用的情况：只改代码写法、只修一个 bug、只回答一个概念问题——这些不涉及本 skill
的四个能力面。用户说「继续」时，先看当前是否有未完成的阶段（见「执行流程」）。

## 硬不变量（任何情况下都成立）

1. **只读先行**：勘察没有完成之前，不写任何文件。判断建立在事实上，不建立在猜测上。
2. **幂等**：同一件事重复执行结果相同。已存在的文件先读后合并，**永不覆盖**；内容冲突
   时报告并停下，不擅自取舍。
   **一处例外要说清**：`AGENTS.md` 里由内核标记圈定的那段内容，归 `templates/agents-kernel.md`
   独占，注入即刷新——那是单一真相源的同步，不算覆盖用户的成果。标记之外照旧一个字节
   不动；而用户若在标记区内写过东西，注入前先提醒他会被覆盖。
3. **密钥零入库**：提交之前先扫描，按 `references/version-control.md` 密钥门控分情况处置（未跟踪排除、已历史轮换、占位继续）。报告位置到行，给出处置建议。凭据只从运行
   环境读取，绝不写进项目里的任何文件。
4. **对外与破坏性动作一次对齐**：建仓库、推送、发布、公开仓库、改写历史——动手前先给
   一份清单让用户过目（清单内容见「硬门控」G4）。**强制推送与改写已推送历史永不自动
   执行**。
5. **平台中立**：不假设操作系统与 shell。不写死 PowerShell / bash / cmd，不写死路径
   分隔符。**不把非 ASCII 的正文经 shell 传递**（例如把中文更新说明拼进命令行或由 shell
   拼请求体，不同 shell 的引用与编码规则不同，且损坏时接口往往仍返回成功）——正确做法
   是写进 UTF-8 文件由脚本按字节发送，发送后回读比对。**路径参数不受此限**：把含中文的
   目录路径作为参数传给脚本是正常且必要的，只需按所在 shell 的规则正确引用。
6. **不写死取值**：账号、路径、版本号、仓库地址一律**现场读**（见「取值从哪来」）。
7. **文档分层**：面向 AI 的文档（`AGENTS.md`）写机制与判据，需要取值时写「从哪里取」；
   面向人的文档（`README` / `CONTRIBUTING`）写具体、可复制的命令与地址。
8. **单一入口**：细节全在 `references/` 与 `templates/` 里，本文件不复述它们。
9. **先确认仓库边界**：目录体量异常大、或里面还嵌着别的仓库时，先停下确认，不要把
   整根纳入。

## 取值从哪来

本文件与所有 reference 都不写死具体值。需要时**从项目自身读**：

| 需要什么 | 从哪里读 |
| --- | --- |
| 项目标识、版本、依赖、发布范围 | 项目的清单 / 元数据文件（存在哪个用哪个） |
| 远端地址、当前分支、提交署名 | 版本控制配置（远端列表、当前分支、署名） |
| 当前登录的账号与作用域 | 对应生态的账号查询命令 |
| 年份（许可证版权行） | 系统时间 |
| 项目的意图与约定 | 项目自己的说明文档与既有配置 |

**项目是这些事实的唯一权威来源**。读不到就说明这项事实还不存在——那就先建立它，
或者问用户，**不要编一个**。

## 能力矩阵：事实 → 做什么

这张表的输入是勘察产出的**全部事实**，不只是生态判定：中间几行由 `ecosystem.kinds`
决定（可以命中多项，一个仓库里有多种生态时逐项分别处理），最后三行由 `git`（远端指向
谁）与 `artifacts`（有没有声明不可发布）决定。**逐行求值，不要只遍历 kinds**——最后
三行恰恰是安全相关的。

**这张表算的是「该做哪几件事」，与项目是新是旧无关。** 已经进过 git、已经有远端、
已经有文档的项目，同样按这张表走——区别只在每一步是「建立」还是「补齐」。具体见下面
「已经有基础的项目」。

| 勘察到的事实 | 版本管理 | 文档套装 | 远端 | 发布 |
| --- | --- | --- | --- | --- |
| `docs-only`（纯文档 / 素材） | 做 | 做 | 做 | **不做** |
| `node` | 做 | 做 | 做 | 视声明而定 |
| `python` / `rust` / `go` / `java` / `ruby` / `php` / `dotnet` / `cpp` | 做 | 做 | 做 | 视声明而定 |
| 其他已识别生态（以勘察 `kinds` 为准，如 `dart` / `swift` / `shell` 等） | 做 | 做 | 做 | 视声明而定（无专章时按通用判据，不硬塞已知类型） |
| `dsh-plugin` / `vscode-extension` / `obsidian-plugin` 等（**插件类**，即被宿主加载的扩展；无专章按判定协议处理） | 做，**产物必须入库** | 做 | 做 | 可发布（见专章） |
| `dsh-skill` | 做 | 做 | 做 | 通常不发布（远端即分发） |
| `unknown` 或 `unrecognized` | **先问用户**这是什么项目；用户说明后，按他声明的生态对应行执行，并把该结论记作本次判定依据 | 同左 | 同左 | 同左 |
| **`git.isRepoRoot` 为假**（目录在别人的仓库里） | **停下**：这些 git 事实属于外层仓库，先问清边界 | 同左 | 同左 | 同左 |
| 已有远端（`git.remote` 非空 **且** 是仓库根） | 只校验与补缺 | 补缺 | **不重建** | 按现状 |
| 远端指向别人的仓库（与项目自身声明的仓库不一致） | 做 | 做 | 只推自己分支 | **不发布** |
| 已声明不可发布（`artifacts.private`） | 做 | 做 | 做 | **不做** |

发布这一列判「能不能发」的依据见 `references/publish.md`；不是所有项目都该发布，
**「不发布」是正常结果，不是失败**。

同一行里若有多列冲突，按更保守的那一列执行（例如既 `docs-only` 又已有远端：远端不
重建，发布仍不做）。

**前四行不是「一定安全」**：`isRepoRoot` 为假这一条**优先于**上面所有行——不管判出
什么生态，只要目录不在自己的仓库根上，就先解决边界问题，再套用上表。

## 已经有基础的项目（最常见的情况）

真实场景里，交到手里的项目**很少是白纸**：多半已经进过 git、已经有远端、已经有一个
写得不太好的 `AGENTS.md`。这时每一件事都从「建立」变成「**先看现状，再补缺口**」——
核心纪律只有一条：**先读，再判断，只补缺的**。

| 步骤 | 项目已有基础时怎么做 |
| --- | --- |
| P3 版本管理 | **不重复初始化**。已有仓库就只补齐缺的属性声明、忽略规则与署名；有未提交改动先存一个存档点。已有远端就**不重建仓库**，只校验它指向谁 |
| P4 文档 | 已有 `README` / `CONTRIBUTING` / `AGENTS.md` 时**先完整读一遍**，按节对照后只补缺失的部分（散文文件的合并规则见 `references/docs-set.md`）。**不要整篇重写**——那会把作者写对的东西一起覆盖掉 |
| P5 远端 | 已有远端就只校验地址、默认分支与元数据；**不要动已有标签与发布说明** |

### 已有的 `AGENTS.md`：三种情况，三种做法

这个文件最特殊——它可能是**别人手写的**，而且往往写得不够好、漏了很多东西。此时既不
能报错放弃，也不能擅自重写。按它有没有内核标记分三种：

| 文件状态 | 做法 |
| --- | --- |
| **不存在** | 直接生成一份完整的（脚本自动填能填的、标出要人写的） |
| **有内核标记**（本 skill 生成或升级过的） | 直接跑脚本刷新：人写过的节保留，纯生成的节按当前事实重新求值 |
| **没有内核标记**（手写的） | **脚本默认不动它**，只输出一份体检：它有哪些节、相对标准结构缺哪些节、哪些可自动补、哪些必须读代码后自己写。要升级结构就加 `--upgrade` |

**手写文件为什么默认不动**：重排别人的文档、把通用样板灌进他精心组织过的结构里，都是
对他人成果的改动。默认沉默地改掉，比不动更糟。所以默认只报告，升级必须显式要求。

**升级只做加法**（`--upgrade` 的保证，已逐条验证）：

- 插入内核段落；
- 补上**可自动补**的缺失节（受条件控制的那些）；
- 需要人写的节**不补**——补进去也只是一堆占位符，不如按项目上下文来写；
- **已有的段落一个字节都不删、不改、不重排**。

升级之后它就成为受管文件，后续刷新走正常路径。

**因此遇到这类项目，P4 的正确顺序是**：先跑一次脚本**看体检报告**（此时它不会改任何
东西）→ 把报告里「需要读代码后自己写」的那几节读代码写出来 → 需要升级结构时加
`--upgrade` → 最后再跑一次确认待填写归零。

## 执行流程

| 阶段 | 动作 | 参考 |
| --- | --- | --- |
| **P0 定位** | 确定目标目录。候选不唯一时问一次 | — |
| **P1 勘察** | 跑勘察脚本，读关键文件补齐意图 | `references/survey.md` |
| **P2 判定** | 用能力矩阵逐行求值，算出做哪几件、跳过哪几件，向用户复述**一句结论**（被委派时用户指调用方，写进回执等指示），并**预告后面要用户定的事**（许可证意向、署名来源、可见性意向、账号/组织、要不要 CI 自动发布——此时只预告不问，P4–P6 再落实） | 本文件 |
| **P3 版本管理** | 文本属性 → 忽略规则 → 署名 → 密钥扫描 → 首次提交 | `references/version-control.md` |
| **P4 文档** | 先过 G7 问清只有用户能定的事；再生成 `AGENTS.md` 并**两个数字归零确认内容完整**，然后写 `README` + `CONTRIBUTING` + `LICENSE`（用户选定后才写）；收尾跑 `scripts/review.mjs`，无[缺]、[待问]全部用 flag 消掉才算完 | `references/docs-set.md` |
| **P5 远端** | 发现通道 → 建仓库（默认私有）→ 推送 → 元数据 → 回读校验 → **回头再跑一次 P4 的脚本补远端相关节（强制）** → 工作流从模板落盘，`review.mjs` 复验 | `references/remote-github.md` |
| **P6 发布** | 仅当 P2 判定可发布。先回四问答案，再预演（清单贴进汇报），再发布，最后回读确认 | `references/publish.md` |
| **P7 验证汇报** | 跑**该项目**勘察到的门禁命令；校验远端与文档；按「做了什么 / 结果如何 / 下一步建议」汇报 | — |

**P7 针对的是目标项目，不是本 skill**。本 skill 自身的自检（`scripts/preflight.mjs`）
只在维护本 skill 时跑，见「怎么用」。

### P7 的一条分支：项目没有门禁命令时

勘察推不出任何构建/测试命令，是**正常结果**（`references/survey.md` 说明了原因），
但它意味着**这次交付没有被验证过**——而不是「验证通过」。

这在新项目上必然发生：刚建起来的项目往往还没有清单文件，也就没有可跑的门禁。此时：

- **不得**汇报为「已完成」「已验证」。真话是「配置好了，但这个项目本身一行代码都没
  验证过」。
- 汇报里必须**显式写明这一点**，不能让用户以为东西是能跑的。
- 「建立第一步门禁」列为下一步建议的**第一条**——哪怕只是一条最朴素的冒烟命令。
- 若项目尚无清单文件，把门禁命令**直接写进 `AGENTS.md` 的构建与验证节**。这不算写死
  取值：门禁命令是**机制**（这个项目怎么验证自己），不是会过期的快照。项目还没有清单
  文件时，写进 `AGENTS.md` 恰恰是把它固定下来的手段。

### P4 不写完不算完成

生成 `AGENTS.md` 之后，脚本会报出**两个**缺口数字，都要看：

| 报告项 | 含义 |
| --- | --- |
| **待填写 N 处** | 模板说「这里要人写」，还留着 `pf:author` 标记 |
| **缺失 N 节** | 模板里有、文件里没有的整节 |

**只盯第一个会误判。** `--upgrade` 刻意不补「需要人写」的节（补进去只是占位符），
所以一份刚升级完、一个项目节都没写的手写文件，`pf:author` 数**立刻就是 0**——只看这个
数字会以为写完了，而项目定位、架构与不变量、构建与验证、硬性规范、测试约定可能一节
都没写。这正是本文警告过的「看起来完整、实际空洞」，而报告本身会变成那个错觉的来源。

因此 P4 的完成条件是**两个数字都归零**：

1. 跑脚本生成或升级 `AGENTS.md`；
2. **读代码，把每一处待填写项写成真实内容**，并删掉对应的 `pf:author` 标记；
3. **补上缺失的节**——它们正是未来的会话真正需要的内容（项目是什么、哪里不能碰、
   怎么验证）；
4. 再跑一次，确认报告「**内容完整**」。脚本会明确打印这句，作为唯一可信的完成信号。

确实写不出的（例如需要用户提供信息才能定的内容），必须**在汇报里逐条列出**，说清哪几节
还是空的。宁可交一份标着「还有 3 节待补」的文件，也不要交一份假装完整的。

### 用户不可达时怎么办

执行者常常**问不到人**（被委派的子代理、无人值守运行、批处理）。此时「指出来让用户
决定」无处可去，而规则如果只写了这一条，就会卡住或产出自相矛盾的成果。

判据与做法：

- **能问就问**——问到了就按各处的「由用户决定」走；
- **问不到时**，凡是「只有用户能定」的事（G7 的三件事、命名、要不要改过时内容）：
  **G7 三件只有留空没有默认值，其余给合理的默认值或明确留空，但绝不自作主张改成不可逆的状态**；
- **把待用户定夺的逐条列进汇报**，写清「我做了什么默认选择、为什么、你可以怎么改」；
- **未完成的事项要能传给下一个会话**：写进 `AGENTS.md` 的待处理事项，或写进汇报。
  **否则下一个会话看到的是一份「干净」的契约，不知道还有尾巴。**

一句话：**问不到人不能成为停顿的理由，也不能成为自作主张的借口**——用默认值推进，
把决定权留清楚。

### P5 之后要回头再跑一次 P4 的脚本

这不是可选项。生成 `AGENTS.md` 时（P4）远端**必然**还不存在，所以「有远端才有」的
内容（发版规则、角色判定、推送与上游跟踪）当时不会出现。P5 建好远端之后**再跑一次
compose-agents**，脚本会按新事实重新求值并把这些节补进去：

```sh
node "<本领目录>/scripts/compose-agents.mjs" "<项目目录>"
```

脚本会报告「按当前项目事实重新求值的节」，据此可以确认补上了。**漏掉这一步，用户拿到
的契约里就永远没有发布那半部分**——那部分恰恰是使用者最容易忘记、也最容易做错的地方。

判定为**插件类**时，`references/plugin-project.md` 在 **P1 之后就该读**（它影响 P3 的产物
入库判断、P4 的发布范围写法、P5 的发布范围核对）——不要等到 P3 或 P6 才读。
该文件同时给出**判定协议**：遇到没有专章的插件生态时，按它的六个问题去查，
再把答案沉淀成新专章（`references/plugins/` 下）。

## 硬门控

这些是**必须先读全文才能动手**的关卡。跳过它们等于凭猜测做事。

| 门控 | 条件 | 必须先读 |
| --- | --- | --- |
| G1 | 任何写入之前 | `references/survey.md`，且勘察已跑完 |
| G2 | 首次提交之前 | `references/version-control.md`；且已按四分案处置、无新增凭据进本次提交 |
| G3 | 写任何文档之前 | `references/docs-set.md` |
| G4 | 建仓库 / 推送之前 | `references/remote-github.md`；且已给用户确认清单（被委派时用户指调用方，写进回执等指示，不直接执行） |
| G5 | 任何发布动作之前 | `references/publish.md`；涉及 npm 时加读 `references/publish-npm.md` |
| G6 | 判定为**插件类**时（`dsh-plugin` / `vscode-extension` / `obsidian-plugin`，或形态上是「被宿主加载的扩展」） | `references/plugin-project.md`（P1 之后即读，见「执行流程」）；有对应专章时一并读 `references/plugins/<生态>.md` |
| G7 | 写任何文档之前（许可证与环境节在 P4 内一次问清） | 见下「只有用户能定的事」 |

### G7：只有用户能定的事，一次问清

有三类事实**脚本推不出来、也不能替用户决定**。它们都在写文档时才需要，所以集中到
一处一次问完——不要分三次打断用户（这与「对外动作一次对齐」是同一条纪律）。

| 要定的事 | 为什么不能替用户定 | 要不到时的做法 |
| --- | --- | --- |
| **许可证** | 这是法律选择，取决于用户想让别人拿它做什么 | 给判据与选项让他选；他选「保留所有权利」就**不落盘 `LICENSE`** |
| **版权行署名** | 涉及真实身份，且会随仓库永久分发 | 从项目自身事实取（仓库声明的作者、提交署名）。**取到明显的占位值（例如操作系统默认用户名）时不要直接用**，回去问 |
| **环境要求的最低版本** | 是在替未来做承诺 | 无可靠依据时**宁可不写这一节**，也不写一个编造的数字 |
| **双语与自动化** | `review.mjs` 的[待问]不经你消不掉（要英文版吗、会给别人用吗、要 CI/自动发布吗） | 一次问完，答案记进 flag；不要默跳，也不要替用户全要 |

没有 `LICENSE`、没有环境要求节，都**不算这次交付的缺陷**——编一个才是。汇报时说明
「这两项等你确认后再补」即可。`review.mjs` 无[缺]即证明：该问的都问了，该补的都补了。

**G4 的确认清单**必须包含这六项，缺一项就不算对齐（以 `references/remote-github.md` 建仓节表格为准，
此处是同一清单的简写，两处改动需同步）：

1. 平台与仓库名（规范化之后的名字，不是目录原名）；
2. **可见性**（默认私有；改成公开要单独说明后果）；
3. 将要推送的远端地址与分支、地址形式（HTTPS 还是 SSH 及理由），以及是否设置上游跟踪；
4. 本次将要推上去的大致内容范围（多少文件、有没有大文件）；
5. **本次跳过哪些动作**（不发布、不改可见性、不动已有标签）。
6. 署名确认：若 P3 用的是临时署名，此处必须已修正（推送后署名永久，见版本管理署名节）。

这份清单与 P2 的那句结论不是一回事：P2 说的是**本次任务做什么**（可能只是补文档），
G4 说的是**这次对外动作具体是什么**（确切的仓库名、可见性、分支）。两者都只说一次，
不重复问同一件事。

**边界**：不可逆或对外的动作（建仓库、推送、发布、改可见性、改写历史）都要走 G4 式
对齐；纯本地写入不需要。

**改可见性（把已有的私有仓库设为公开）是其中最危险的一个**：它不像新建那样可以「先
清理再推」——仓库里已经有历史，一公开，**整段历史一次性暴露**，而且改回私有也收不回
已经流出去的内容。它的处理步骤见 `references/remote-github.md` 的「可见性变更」一节，
必须完整走完才能执行，默认答案是「不做」。

## 怎么用

**先读协议，再跑脚本**——G1 要求任何写入前读完 `references/survey.md`，而勘察本身是
只读的，所以先跑勘察不会违规，但读协议要在动手之前完成。

下面三条命令里的 `<本 skill 目录>` 指这个 skill 自己的位置（本文件所在目录），
`<项目目录>` 指被处理的那个项目。两者通常不同，**命令可以在任意工作目录下执行**，
因为路径都是显式传入的。

```sh
# 1. 勘察目标项目（只读，不写任何东西）
node "<本 skill 目录>/scripts/survey.mjs" "<项目目录>" --markdown

# 2. 按 P2 的判定执行 P3 到 P7（详见上面的执行流程与各 reference）

# 3. 把通用内核注入目标项目的 AGENTS.md（幂等；标记区归内核独占，注入即刷新）
node "<本 skill 目录>/scripts/compose-agents.mjs" "<项目目录>"
```

下面这条**只在维护本 skill 自身时**运行，与目标项目无关（它按脚本自身位置推导检查
对象，不接受项目目录参数）。它不是 P7：

```sh
# 自检本 skill：内核是否与模板一致、引用是否断链、是否含 emoji
node "<本 skill 目录>/scripts/preflight.mjs"
```

各脚本都只用运行环境内置的模块，零依赖，跨平台。含中文的目录路径作为参数传入是
正常用法，按所在 shell 的规则正确引用即可。

## 文件地图

| 文件 | 何时读 |
| --- | --- |
| `references/survey.md` | **每次任务开始**。勘察协议与「事实 → 动作」判定表 |
| `references/version-control.md` | 配置或使用版本管理时。产物判据、忽略规则、署名、提交纪律、回滚、密钥门控 |
| `references/remote-github.md` | 涉及远端仓库时。通道发现、建仓、推送、元数据、版本节点与发布说明、协作模板、CI |
| `references/docs-set.md` | 写任何文档时。各文档的读者、职责、写法与同步纪律 |
| `references/publish.md` | 任何发布决策。能不能发、发到哪、版本语义、抬版本的判据、安全发布 |
| `references/publish-npm.md` | 涉及 npm 时。基础操作、各种既有状态怎么接、适用范围与常见误解 |
| `references/plugin-project.md` | 判定为插件类时。共同性质、判定协议（六个问题）、无专章时怎么办、新增专章的模板 |
| `references/plugins/<生态>.md` | 该生态的专章：清单、两个标识、宿主加载什么、版本约束、打包发放、特有坑 |
| `templates/agents-kernel.md` | 生成 `AGENTS.md` 时（由脚本读取，**不手工编辑**；标记区归它独占） |
| `templates/agents-project.md` | 新建 `AGENTS.md` 时作为骨架 |
| `templates/readme.md` | 新建 `README` 时作为骨架 |
| `templates/contributing.md` | 新建 `CONTRIBUTING` 时作为骨架 |
| `templates/ci-check.yml` | 给目标项目落 CI 检查（复制到 `.github/workflows/check.yml`，按 TODO 换取值） |
| `templates/ci-release.yml` | 给目标项目落发布自动化（复制到 `.github/workflows/release.yml`，先建 `RELEASE_TOKEN`） |
| `templates/license-mit.txt`、`templates/license-isc.txt`、`templates/license-bsd-2-clause.txt`、`templates/license-bsd-3-clause.txt`、`templates/license-unlicense.txt` | 短许可证标准全文（落盘时替换占位符，Unlicense 例外） |
| `scripts/survey.mjs` | P1 勘察 |
| `scripts/compose-agents.mjs` | P4 生成、刷新或升级 `AGENTS.md`（`--status` 只看现状，`--upgrade` 升级手写文件） |
| `scripts/preflight.mjs` | **维护本 skill 自身时**的自检（与目标项目无关） |
| `scripts/selftest.mjs` | **维护本 skill 自身时**的行为自检：造 fixture 实跑，断言每条判定（与目标项目无关） |
| `scripts/release-notes.mjs` | 写发布说明——`references/remote-github.md` 那条「非 ASCII 正文不经 shell」规则的落地工具 |
| `scripts/check-badges.mjs` | 检查 README 里的徽章是否真的能显示（`references/docs-set.md` 那条「只写能显示的」的落地工具） |
| `scripts/sync-toc.mjs` | 让 Markdown 的目录与标题保持同步（`references/docs-set.md` 那条「派生内容用工具同步」的落地工具） |
| `scripts/review.mjs` | P4/P5 交付门禁：无[缺]、[待问]用 flag 消掉才算写完（治“写完就忘”） |

## 边界

**做**：版本管理的建立与维护、远端仓库的建立与元数据、文档套装的建立与同步、发布通道
的配置与发布本身。

**不做**：部署与服务器配置、容器编排、运行环境安装、代码本身的编写与重构。

**永不自动做**：公开一个私有仓库、强制推送、改写已推送历史、删除已发布版本、替用户
决定许可证、替用户编造署名或账号。

## 汇报纪律

- 每个阶段结束时同步进度，不要闷头做完再说。
- P2 的判定要复述给用户（做了哪些、跳过哪些、为什么），因为判定可能与用户预期不同。
- 收尾按「做了什么 / 结果如何 / 下一步建议」汇报。
- 用户可见的改动完成后，**只报版本号**；文档与流程细节不必复述。
- 跳过某件事时要说清**为什么跳过**——「不发布」和「忘了发布」在用户看来是一样的。

# 发布总纲

## 何时读本文件

本文件是**发布决策的总纲**，跨技术栈通用。凡任务涉及「把项目交出去」，都读本文全文：

- 用户说「发版」「发出去」「发布这个项目」「让别人也能装」；
- 要判断一次改动该不该抬版本号；
- 要判断最低支持版本（兼容性下限）要不要跟着上游一起升；
- 要配置或排查自动发布通道、凭据与发布前检查；
- 发布已经出错，要找修正路径。

本文只管**决策与通用规则**。某一个具体制品库的命令、字段、场景化接入，见文末
「与其他文件的关系」指向的专章。**判断与执行分开**：读完本文应先能回答「发不发、发到哪、
发什么号」，再去查那个生态的具体操作。

## 先回答四个问题

**总原则：发布不是一个动作，是一个决策链。**

这条链有四问，必须按顺序回答；前一问的答案决定了后一问还成不成立。

| 顺序 | 问题 | 一句话判据 | 答错的代价 |
| --- | --- | --- | --- |
| 1 | 能不能发？ | 有没有一个「别人可以拿去用」的边界 | 给本就该自用的项目套上长期维护义务 |
| 2 | 该发到哪？ | 目标消费者在哪个生态 | 发到了没有消费者的地方，等于没发 |
| 3 | 版本怎么走？ | 这次改动使用者会不会看到 | 使用者收不到更新，或收到无意义的更新 |
| 4 | 怎么安全地发？ | 凭据从哪来、谁能代表账号发布 | 凭据泄露、发到错误身份、污染版本号 |

**跳过前三问、直接执行第四问，是发布事故的主要来源。** 因为第四问是整条链上唯一产生
不可逆副作用的动作：一旦上传或推送，版本号就被占用、制品就公开可下载。前三问都是纯推理，
成本极低，省掉它们的代价却要发布之后来承担。**先想清楚，再动手。**

下面各节按这四问展开：「能不能发」「该发到哪」两节回答前两问，
「版本号语义」「什么时候该抬版本号」「跟随版本与最低支持版本」三节回答第三问，
「怎么安全地发」及之后两节回答第四问。

## 能不能发：可分发形态

**判据：项目里有没有一个「别人可以拿去用」的边界。**

边界的含义是：存在一样东西，能被与本项目无关的人在**不了解你的内部约定**的前提下拿走
并使用。有边界 → 可发；没有边界 → 不可发。

可分发形态的典型样子（不限于此）：

| 形态 | 别人拿去做什么 | 边界是什么 |
| --- | --- | --- |
| 可被依赖的库 | 在自己的代码里调用 | 公开的接口 |
| 可执行的工具 | 在自己的环境里运行 | 命令行契约 |
| 可安装的扩展 / 插件 | 装进宿主程序里启用 | 宿主约定的接入点 |
| 可被引用的数据集 / 文档包 | 在自己的材料里引用 | 稳定的标识与版本 |

反例（这些**通常不该发布**）：

- **纯应用 / 服务**：别人是来用你的服务，不是来装你的包。它的交付物是运行中的服务，
  需要操心的是部署与运维，而不是制品库。
- **私有脚本集合**：内容与本机环境、私人路径、私人凭据强绑定，拿出去就跑不起来。
- **只有作者知道怎么跑的研究目录**：没有可复现的入口，别人拿到也复现不了。

**结论：「不发布」是正常且常见的结果，不是失败。** 判断为不可发时说清理由即可收工；
不要为了「有一个发布流程」而硬套一层分发外壳——那是需要长期维护却没人受益的负担。

**从仓库源获取与从制品库获取是两条不同的分发路径**，不要混为一谈：

| | 从仓库源获取 | 从制品库获取 |
| --- | --- | --- |
| 方式 | 克隆仓库，或直接从仓库地址安装 | 用包管理器从制品库安装 |
| 前提 | 仓库可读即可 | 必须先把版本发到制品库 |
| 更新感知 | 比提交 | 比版本号 |
| 适用 | 想跟最新代码、参与开发的人 | 想要稳定版本、不想跟源码的人 |

所以**「没有发布」不等于「别人拿不到」**：只要你推送了，从仓库源获取的人就已经拿到了。
「这个项目还没发过版」本身不是必须发布的理由。

## 该发到哪：生态对照

**判据：目标消费者在哪个生态，就发到那个生态的制品库。** 先确定「谁是使用者」，
再选制品库；不是先挑一个制品库，再看它能装什么。

| 语言 / 生态 | 默认的公共制品库 | 发布动作的本质 |
| --- | --- | --- |
| Python | 官方包索引（PyPI） | 上传一个制品（源码包或轮子） |
| JavaScript / TypeScript | npm 公共制品库 | 上传一个包 |
| Rust | crates.io | 上传一个 crate 制品 |
| Go | **无中心上传动作**，由模块代理按版本标签取 | **打一个版本标签**，不需要上传 |
| Java / JVM | Maven Central 一类中央仓库 | 上传构件（含坐标与元数据；校验最重，无可信发布） |
| Ruby | RubyGems | 上传一个 gem（有可信发布；同号不可重推，yank 后也不行） |
| PHP | Packagist | 登记包，由版本控制标签驱动（索引不托管制品；稳定版映射不可变） |
| .NET | NuGet | 上传一个包（无可信发布；不可删，只可下架或废弃） |
| Dart | pub.dev | 上传一个包（有测试演练 flag；发布永久，7 天内可撤回） |
| Swift | 无中心库（Git 即真相，可选企业级注册表） | **打一个版本标签**，不需要上传 |
| 无语言归属的通用资产 | 随形态而定：制品库的通用仓库、发布页附件、容器镜像仓库 | 上传一个带版本的制品 |

**特别提醒：不要强行把所有生态套进「上传一个包」的模型。** 有的生态没有中心化的上传
动作，它的「发布」就是**在版本控制里打一个版本标签**，由生态的模块代理或索引去取；
也有的生态以版本控制标签为唯一真相，制品库页面只是它的镜像。判断方法很简单：
**问「发布这个动作最终改变了什么」**——是某个库里多了一个制品，还是版本控制里多了一个
标签。两者的自动化方式、可撤销性、以及「发错了怎么办」都不相同。

**跨生态分发**（例如把非 JavaScript 的东西发到 npm 一类生态）技术上常常可行，
但**只有在你确实想触达那个生态的使用者时才有意义**。若使用者本来就在原生态里，
跨发只会制造一个需要同步维护的第二份制品与第二套版本号。

各生态对「三档版本语义对应哪一段数字」「版本标签叫什么形状」有自己的约定，
**从该生态的惯例与项目的清单文件里确认**，不要跨生态套用。

## 没有专章的生态：现场查什么（六问 + 门禁四项）

专章只覆盖 npm / Python / Go / Rust。有专章的按专章走；没有专章的**不要凭记忆拼命令、
也不要照搬已有专章的那一套**，按下面六问去该生态的官方文档查，再把答案沉淀成新专章
（沉淀模板见 `references/plugin-project.md` 第九节的写法，要求只写特有事实、不重复通用结论）。

| # | 现场问题 | 要翻到的章节 | 起点链接 |
| --- | --- | --- | --- |
| 1 | 真相在哪？制品库多一个制品，还是版本控制多一个标签，还是登记加标签？ | 发布模型、提交包指南 | Go 模块发布、包索引说明、Swift 包发布指南 |
| 2 | 号在哪？清单文件名、版本字段、标签形状？ | 清单参考、版本章节 | Go 模块参考、中央仓库坐标要求、gem 规范参考、Composer 版本章节、Dart 包清单 |
| 3 | 怎么预演？演练 flag、本地安装、测试站、服务端校验哪一层？ | 发布指南的验证一节 | 中央仓库发布指南、Dart 发布命令 |
| 4 | 凭据哪来？可信发布登记，还是长期令牌，还是无认证？ | 可信发布、令牌管理 | RubyGems 可信发布、NuGet 作用域令牌、Dart 验证发布者 |
| 5 | 号能重用或删除吗？不可覆盖哪条写的？ | 不可变版本、删除政策 | 包索引不可变版本说明、NuGet 废弃包、gem 下架指南 |
| 6 | 错了怎么修？撤回、下架、废弃、软删除、走私有路线，各是哪种？ | 修正与托管章节 | NuGet 托管包总览、自建 gem 服务指南 |

查不到的项如实说明并请用户确认，不要猜。凡拿不准，就把「该生态的发布流程需要现场核实」
明确写进汇报，而不是给出一串看似专业、实则未经核对的命令。**不知道就说不知道，比给错命令好。**

发布前最小人工核对（清单、预演、认证、回读四项，贴结果不贴凭据）：

1. **清单**：说出清单文件名、版本字段、标签形状三件套并贴出待发值，标签与字段数值同源；
2. **预演**：跑该生态的预演并贴结果（演练 flag 零警告、服务端校验通过、测试站可见、
   本地命令全绿；登记型生态跑清单校验），逐个确认待公开文件；
3. **认证**：说出凭据种类与来源（可信发布登记了哪个仓库与工作流？长期令牌在哪个机密存储？
   无认证的说出推送权限主体），绝不贴凭据明文，长期令牌只放环境变量或平台机密存储；
4. **回读**：发布成功后去制品库或代理查版本号、元信息、文件清单一致（容忍索引与扫描延迟，
   先查后动，不断网重推）。失败按“先查在不在，在则按已发布处理，不在则修因重发不改号”。

各生态有无可信发布的速查（现场仍以官方文档为准）：RubyGems 与 Dart 有，
Maven Central 与 NuGet 无（长期令牌），Go 与 Swift 无需（能推送即能发）。

## 版本号语义

版本号是**给使用者的承诺**，不是内部流水账。它只回答一个问题：
**从上一个版本到这个版本，使用者要付出什么代价。**

### 三档语义

| 档 | 判据：消费者会不会被迫改自己的代码 | 为什么要如实标注 |
| --- | --- | --- |
| 破坏性变更 | 会——不改就用不了，或行为变了 | 不标出来，使用者的构建会在某次自动升级后突然坏掉 |
| 新增 | 不会，但多了能力 | 旧的用法原样有效，使用者可以选择用新能力 |
| 修补 | 都不会——行为与接口对使用者完全等价 | 修缺陷、内部调整 |

**判据只有一条：消费者会不会被迫改自己的代码。** 不要用「改了多少行」「花了多久」来衡量：
改动规模与版本档位无关——一行删除可能砸掉别人的调用，几百行重构可能对使用者完全透明。

### 三条不变量

- **单调递增，永不复用**：同一个版本号不能发第二次。用掉的号是历史，不是可回收资源。
- **已发布的版本通常不可删除**，只能发新版本修正。原因不是平台苛刻：**别人的依赖锁定
  文件里可能已经写了这个号**，删掉会让他们的构建在毫无改动的情况下无故坏掉。
- **版本号的唯一权威来源是项目清单文件里的那一个字段**。不要在文档、脚本、代码里各写
  一份——多写一处就多一处会漂移的副本，而版本号一旦漂移，使用者拿到的判断依据就是错的。
  打标签时**标签名要与该字段一致**：形状按生态惯例，数值必须同源。

## 什么时候该抬版本号

**判据：使用者会不会因此看到变化。** 看得到 → 抬；看不到 → 不抬。

**日常与发版分开**：抬版本号归日常（每次用户可见改动提交时就抬，保证仓库里的号永远最新），
打标签与写发布说明归发版里程碑（只有明确说发版才做，把攒了的号一次推出去）。
混在一起就会碎标签满天飞——标签是里程碑，不是提交的附庸（本 skill 自身就是例子：
日常只提交推送，发版才打标签，见本仓库 `AGENTS.md` 版本节点节）。

| 要抬 | 不抬 |
| --- | --- |
| 行为、界面、交互、报错文案 | 重构 |
| **随包发出去的说明文档**（会展示在制品库页面上，也随包分发） | **不进包的内部文档**（只给协作者、给 AI 看的那些） |
| 面向使用者的兼容性声明变化 | 测试、构建脚本 |

### 最容易判错的一行：两种「文档」

同样叫「文档」，它们的读者完全不同：

- **随包分发的那种是产品的表面**：使用者按它判断这个包能不能用、怎么装、怎么配、
  有什么限制。改一个字都算用户可见——因为它本身就是使用者看到的产品说明。
- **只存在于仓库里的内部文档**：使用者根本接触不到，它服务的是协作者与 AI。

所以前者必须抬版本，否则会出现**「仓库里的说明已经改了，制品库页面上还是旧的」**：
使用者照着旧说明装，装出问题还找不到原因。后者不抬，因为它不进包，制品库上本来就没有它。

### 反过来也要提醒：看不到的改动硬抬，是噪音

用户看不到的改动（重构、测试、构建脚本）如果也抬版本，会在制品库上多出一个**内容与
上一版完全相同的版本**。使用者收到更新提示、下载安装，然后发现没有任何变化——
几次之后就不再相信更新提示了。**版本号的信用，是靠「每次跳动都对应真实变化」积累的。**

### 三个地方各自怎么感知更新

| 渠道 | 靠什么判断「有更新」 |
| --- | --- |
| 从制品库安装的人 | 比版本号 |
| 从仓库源获取的人 | 比提交 |
| 版本节点 / 发布说明 | 看标签与说明——想知道「这版改了什么」的人读它。**所以正文必须回答两件事：更新了什么、修复了什么**，用使用者看得懂的话；内部编号、任务代号、对使用者无影响的重构都不该出现在这里 |

**结论：抬版本号主要是给制品库渠道用的。** 仓库渠道在你推送的瞬间就已经拿到新代码，
它不需要版本号来通知——这也解释了为什么「没抬版本」不等于「别人拿不到」。

## 跟随版本与最低支持版本（两个不同的概念）

这是极易混淆、后果严重的一节。两者都是「版本」，都写在清单文件附近，
但回答的是完全不同的问题。

| | 跟随版本 | 最低支持版本 |
| --- | --- | --- |
| 是什么 | 构建与测试所依赖的宿主 / 依赖版本 | 使用者的环境还能不能跑起来的最低下限 |
| 写在哪 | 开发依赖声明 | 兼容性声明 + 面向使用者的说明文档 |
| 何时变 | 每次上游升级都跟上去 | **只在真不兼容时上移** |
| 谁看得到 | 只有开发者 | 使用者 |

### 关键规则

- **最低支持版本只上移、不下移，且默认不动。** 下移等于向使用者承诺一个你没有验证过的
  环境；默认不动，则是因为**升级上游并不会自动抬高下限**。
- **升级上游之后，项目在新版上照常工作、甚至继续在新版上开发，恰恰说明旧的下限依然
  成立。** 这时**不要动它，也不要问要不要改成最新版**：「我能跑在新版上」与「我要求新版」
  是两件事——前者是开发者的便利，后者是使用者的门槛，抬高门槛只会挡住本可使用的人。
- **只有两种情况才上移**：
  1. 采用了只有新版才有的能力——旧版没有这个符号或接口，加载即失败；
  2. 在旧版上**实测**失败。

  除此之外的理由（「新版更好」「保持一致」「反正没人用旧版」）都不成立。
- **上移时两处必须一起改**：兼容性声明里的下限，与面向使用者的说明文档里的那一行。
  它们是**同一事实的两种表达**，漏改一处就会出现「文档说支持、装上去却报错」的错配，
  而这种错配使用者无法自行诊断。
- 面向使用者的那行**必须写具体版本号**：使用者需要一个能拿来对照自己环境的数字。
  「最新版」「较新版本」这类模糊说法等于没写。

## 怎么安全地发

按优先级排序：能用自动发布通道就用；不能再用长期令牌；人工验证码只作为最后的交互式兜底。

| 方式 | 机制 | 前提 | 定位 |
| --- | --- | --- | --- |
| 基于身份的自动发布（OIDC / 可信发布） | 构建平台在运行时**签发一次性凭据**，用完即废 | 在制品库平台上登记「哪个仓库的哪个工作流可以代表我发布」 | 首选 |
| 长期令牌 | 一个长期有效的密钥 | 必须只放在**环境变量或平台的机密存储**里 | 次选 |
| 人工验证码（双因素） | 账号持有人在自己的终端输入一次性验证码 | 需要人在场 | 交互式兜底 |

**长期令牌绝不进版本库**：不写进文件、不写进配置、不写进提交信息，也不贴进对话。
一旦进了版本库，它就成了历史的一部分，删掉某个提交并不能让它失效——
**正确的处置是立即在平台上吊销并重新签发**。

**人工验证码属于交互式操作**，需要账号持有人在自己的终端执行。自动化流程**不该依赖
它**：依赖它的「自动化」在无人值守时一定会断，而断的那一刻通常正是你在赶一个紧急修复。

### 能力边界：自动发布通常只覆盖「发布」这一个动作

这一点最容易被误解。自动发布通道打通之后，**能自动做的只有「发布」**；下面这些通常
仍然需要人工验证码或网页操作：

- 撤销发布、删除或废弃某个版本；
- 修改包的权限、协作者、可见性；
- 修改账号设置，或调整发布者登记本身。

所以**不要把「已经能自动发布」误解为「所有账号操作都自动化了」**。规划回滚与应急流程时，
要按「这些操作需要人在场」来设计，否则真出事时会发现退路比预想的窄。

### 发布前的最后一道检查：看清要发出去的到底是什么

大多数生态都提供**预演手段**：只打包不发布，并列出将要包含的文件。发布前跑一次，
**逐个确认清单里的每一项都是你打算公开的东西**。

要专门提醒一条：**发布范围声明对「从仓库源安装」的场景同样生效**。也就是说，
**仓库里存在某个文件，不代表装到使用者机器上会有它**——范围声明会把它排除掉；
反过来，范围声明之外的东西也不会因为「它在仓库里」就自动跟着走。这一条同时解释了两类
事故：使用者抱怨「装了却没有那个文件」，以及你把不该公开的东西发上了制品库。
例外：Go（打标签即发布、无白名单筛选）、Swift（同理）与 Obsidian 插件（分发的是发布附件、
无白名单）不适用本条，按各自专章处理；Packagist 同理（索引不托管制品，无上传内容可筛）。

### 专章的「事实来源」标记：外部的值会过期

带外部易变事实的章节（`references/publish-<生态>.md` 与 `references/plugins/` 下各专章）
末尾都有一节「事实来源」，以及一个机器可读的核对标记：

`<!-- <scope>-verified: date=YYYY-MM-DD [key=value ...] -->`

用法只有三条：

- **动手前扫一眼日期**。超期不等于判据失效，但等于「正文里的具体值只是上次看到的样子」；
  先按该节写明的来源重核一遍，再照它执行。
- **失败时第一动作是重核，不是重试**。发布报「无权 / 不存在 / 字段非法」时，先怀疑平台侧
  改了规则或字段名，按该节的来源核对后再动手（各专章的翻车表已是这个顺序）。
- **核完把 `date=` 改成重核当天**，取值有变的顺手更新正文；格式与校验规则以
  `scripts/preflight.mjs` 的实现为准（它是唯一真相源，坏格式、错 scope、未来日期都会自检失败）。

### 发布后的确认

发布动作返回成功之后，**去制品库上确认新版本确实出现了**，并核对它的元信息
（版本号、说明、文件清单）与你预期的一致。

注意一个正常现象：**有些制品库在发布后会先做一轮安全扫描，短时间内查询可能返回
「不存在」，这属于正常延迟，不是发布失败。** 遇到这种情况先等一会儿再查，不要立刻重发——
重发通常会因为版本号已被占用而失败，或者更糟：让你误以为需要换一个号。

## 发布失败与中断

发布不是原子操作，中途会断。「发出去了没有」必须**回读确认**，不能靠命令退出码判断。

| 中断情形 | 判断与处置 |
| --- | --- |
| 命令报错退出 | 先查制品库上这个版本**在不在**（有的工具先上传再报错，实际已经上去了）。在 → 按已发布处理；不在 → 修掉原因再发，**不要改版本号重试** |
| 命令超时 / 断网，没有明确结果 | 同上，先查后动。**最忌讳的是直接重跑**——重跑可能撞上「版本号已占用」，也可能把同一个号推两遍 |
| 上传成功但校验失败 | 按失败处理，但**先别急着删**：查清是内容问题还是校验配置问题。内容有问题就发新版本修正 |
| 多包仓库只成功了一部分 | **逐个包确认状态**，不要假定「一起发的就一起成功」。已经发出去的那几个包按已发布处理，剩下的单独补发；在汇报里如实说明哪些成功、哪些没有 |

**回读的方法是「查制品库上有没有这个版本」，不是「看命令输出」**——同一件事在不同工具、
不同网络条件下表现不同，只有制品库自己的状态是权威。

发布完成后也要回读一次，确认新版本确实可见（注意有的制品库会在发布后先做安全扫描，
短时间内查询返回「不存在」属于正常延迟，见上文）。

## 预发布版本

有些项目需要先发一个「候选版」给别人试用，再发正式版。这件事**有专门的机制**，不要用
「发一个正式版本再撤回」来模拟。

要点：

- 预发布版本在版本号里有额外的标记段，语义上**低于**同号的正式版；
- **关键风险：不要让它顶掉「最新稳定版」的位置**。制品库的「最新」指针若被预发布版
  覆盖，所有装默认版本的使用者都会拿到候选版——这是把试用版推给全体用户的经典事故。
  发布时必须显式声明这是预发布，让制品库把它放到另一个通道；
- 正式版发布后，预发布版本通常应被废弃或移除，避免使用者误装。

预发布版本的标记写法、以及「怎么让制品库不把它当最新版」，**各生态规则不同，以该生态
官方文档为准**（npm 的对应规则见 `references/publish-npm.md`）。

## 发布后的修正路径

发布不可逆，但**可以往前修正**。总原则是：**只用新版本修，不回改历史。**

| 情况 | 可行手段 | 限制 |
| --- | --- | --- |
| 刚发布就发现问题 | 尽快发一个修正版本 | 版本号必须继续往前；不要试图覆盖已发布的号 |
| 某个版本有严重缺陷 | 把那个版本标记为「已废弃」，让使用者看到警告 | **不要删除**：删掉会让已经锁定了这个号的使用者构建失败 |
| 版本号发错了（发成了不该发的号） | 只能继续往前发 | 那个号已被占用，无法回收；后续版本按正确语义继续递增 |
| 制品内容里混入了不该有的文件 | 发新版本，并从发布范围里排除它 | 若混入的是凭据，**先吊销凭据**再发新版本；旧版本的下载记录无法收回 |

两条通用纪律：

- **优先「告知」而不是「抹除」**：废弃标记让使用者知道该升级了，删除则让他们无从判断。
- **撤不回来的东西按撤不回来处理**：已经公开的凭据一律视为泄露，吊销并重发是一条独立于
  版本号的动作，不要指望靠发一个新版本把它盖掉。

各生态的修正动作映射（细节见专章或现场查官方文档）：

| 生态 | 修正动作 | 含义 |
| --- | --- | --- |
| npm / Python / Rust 通用 | 废弃标记（yank 一类） | 新解析不再选用，存量锁定继续可用；不要删除 |
| Go | retract 声明 | 在清单里声明撤回版本区间，再发更高新版让它生效；旧版仍保留可用 |
| RubyGems | yank | 从索引摘除；同号不可重推，删了也一样 |
| Packagist | 软删除带原因 | 不再解析已删版；稳定版映射不可变，只能发下一版 |
| NuGet | 下架 / 废弃 | 下架后搜不到仍可还原安装；废弃带替代包提示；删除极受限不推荐 |
| Dart | 撤回 / 停更 | 撤回有 7 天窗口；停更打标并可推替代包 |

## 与其他文件的关系

本文讲通用决策。**各生态的操作细节、字段含义、场景化接入，见对应的专章**：

| 生态 | 专章 |
| --- | --- |
| npm | `references/publish-npm.md` |
| Python | `references/publish-python.md` |
| Go | `references/publish-go.md` |
| Rust | `references/publish-rust.md` |

npm 在本文中只作为「生态中的一个例子」出现，其命令与字段不在这里展开。

**没有专章的生态，现场查该生态的官方文档**，而不是凭记忆拼命令、也不是照搬已有专章的那一套。

「该发到哪」那张对照表只回答「发给谁、发到哪」，**不包含任何
具体命令、字段或校验步骤**——那些在上表各专章里。凡拿不准，就把「该生态的发布流程需要现场核实」明确写进汇报，而不是给出
一串看似专业、实则未经核对的命令。**不知道就说不知道，比给错命令好。**

## 汇报纪律

发布完成后向用户汇报，**只报版本号**——用户在已安装列表或制品库页面上看的就是这个号，
过程的复述对他们没有信息量。

同时说明**「用户能在哪里看到这次更新」**：包管理器的更新提示、制品库的版本页面、
仓库的版本节点或发布说明。用户需要知道去哪里确认，而不是听一遍过程。

若本次发布伴随使用者可见的变化，用一句话说清变化是什么；不要把提交记录抄一遍。

## 事实来源

本文件的判据（能不能发、发到哪、四问与门禁四项）、以及「生态对照」表里各制品库的行为
（可信发布的有无、标签驱动还是上传驱动、可否删除或撤回），均来自各生态官方发布文档的
对应章节；预演与回读的做法来自发布工具的官方说明。核对方是**逐条读原文**，不是照抄记忆。

**核对版本（对表里的制品库行为有疑问时重核）**：

<!-- publish-verified: date=2026-09-16 -->

上面一行是机器可读的核对标记，格式不要改；用法见本文件的「专章的『事实来源』标记」一节。
表里每个生态的取值都只是**上次核对到的样子**——它属于易变事实，动手前按该生态的官方文档
重核一遍，尤其是「可否删除 / 能否撤回 / 有没有可信发布」这三类会随平台政策变的项。
